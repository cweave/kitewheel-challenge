import React from 'react';
import Navigation from './Navigation';

const Header = () => {
	return (
		<div className="nav-bar">
			<h1>kitewheel</h1>
			<Navigation />
		</div>
	);
};

export default Header;
